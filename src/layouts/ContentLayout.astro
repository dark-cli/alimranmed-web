---
import BaseLayout from "./BaseLayout.astro";

interface Props {
  title: string;
  description?: string;
  image?: string;
  category?: string;
  backHref?: string;
  backLabel?: string;
}

const { title, description, image, category, backHref, backLabel } = Astro.props;
---

<BaseLayout title={title} description={description} image={image}>
  <div class="content-page">
    {(backHref || category) && (
      <div class="breadcrumb container">
        {backHref && backLabel && (
          <a href={backHref}>← {backLabel}</a>
        )}
        {category && !backHref && (
          <span class="crumb-category">{category}</span>
        )}
      </div>
    )}

    <header class="page-header container">
      <h1>{title}</h1>
      {description && <p class="lede">{description}</p>}
    </header>

    <article class="container">
      <div class="prose">
        <slot />
      </div>
    </article>
  </div>
</BaseLayout>

<style>
  .content-page { padding-block: 2rem 4rem; }
  .breadcrumb {
    font-size: 0.9rem;
    color: var(--ink-muted);
    margin-bottom: 1rem;
  }
  .breadcrumb a { color: var(--ink-soft); }
  .crumb-category { text-transform: uppercase; letter-spacing: 0.06em; }
  .page-header { margin-bottom: 2rem; text-align: center; }
  .page-header h1 { margin-bottom: 0.4em; }
  .lede { color: var(--ink-soft); font-size: 1.1rem; max-width: 60ch; margin: 0 auto; }
</style>
